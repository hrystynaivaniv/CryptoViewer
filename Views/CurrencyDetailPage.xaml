<UserControl x:Class="CryptoViewer.Views.CurrencyDetailPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <Button Content="Back to List" 
                    Command="{Binding BackCommand}" 
                    HorizontalAlignment="Left" 
                    Padding="10,5" 
                    Cursor="Hand"/>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,10">
                <Image Source="{Binding Currency.ImageUrl}" Width="40" Height="40" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Symbol}" FontSize="20" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>

            <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="{Binding PriceDisplay}" FontSize="22" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ChangeDisplay}" Foreground="{Binding ChangeColor}" FontSize="18" Margin="0,5"/>
                    <TextBlock Text="{Binding VolumeDisplay}" FontSize="16" Foreground="DimGray"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Price History (Last 7 Days)" Margin="0,20,0,5" FontWeight="Bold" FontSize="16"/>
            <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Height="300" Background="White">
                <oxy:PlotView Model="{Binding ChartModel}" 
                              Controller="{Binding ChartController}" 
                              Background="Transparent"/>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>