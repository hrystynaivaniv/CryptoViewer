<UserControl x:Class="CryptoViewer.Views.CurrencyDetailPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <Button Content="← Back" Command="{Binding BackCommand}" HorizontalAlignment="Left"/>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,10">
                <Image Source="{Binding Currency.ImageUrl}" Width="40" Height="40" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="{Binding Symbol}" FontSize="20" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="Gray"/>
            </StackPanel>

            <TextBlock Text="{Binding PriceDisplay}" FontSize="18"/>
            <TextBlock Text="{Binding ChangeDisplay}" Foreground="{Binding ChangeColor}" FontSize="16"/>
            <TextBlock Text="{Binding VolumeDisplay}" FontSize="16"/>

        </StackPanel>
    </Grid>
</UserControl>