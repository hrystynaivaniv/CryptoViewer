<UserControl x:Class="CryptoViewer.Views.MainCurrencyPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Background="White">

    <UserControl.Resources>
        <Style x:Key="CoinCardStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="CardBorder" 
                                Background="White" 
                                CornerRadius="12" 
                                Margin="0,0,0,10" 
                                Padding="15" 
                                BorderBrush="WhiteSmoke" 
                                BorderThickness="1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#28a745"/>
                                <Setter TargetName="CardBorder" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#28a745"/>
                                <Setter TargetName="CardBorder" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28a745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#28a745"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#28a745"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Crypto Market" FontSize="32" FontWeight="ExtraBold" Margin="0,0,0,20" Foreground="Black"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Height="40" VerticalContentAlignment="Center" Padding="15,0"
                     FontSize="14" BorderBrush="White">
            </TextBox>

            <Button Grid.Column="1" Content="Converter" 
                    Command="{Binding OpenConverterCommand}"
                    Style="{StaticResource ButtonStyle}" Margin="10,0,0,0"/>
        </Grid>

        <ListView Grid.Row="2" 
                  ItemsSource="{Binding Currencies}" 
                  ItemContainerStyle="{StaticResource CoinCardStyle}"
                  Background="Transparent" 
                  BorderThickness="0"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>

                        <Border Width="40" Height="40" Background="White" CornerRadius="20">
                            <TextBlock Text="{Binding Symbol}" 
                                       VerticalAlignment="Center" HorizontalAlignment="Center" 
                                       FontWeight="Bold" Foreground="#28a745" FontSize="10"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0">
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="Black"/>
                            <TextBlock Text="{Binding Symbol}" Foreground="Gray" FontSize="12" TextAlignment="Left"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0">
                            <TextBlock Text="{Binding CurrentPrice, StringFormat={}{0:N2} $}" 
                                       FontSize="16" FontWeight="Bold" HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding PriceChange24h, StringFormat={}{0:P2}}" 
                                       FontSize="12" HorizontalAlignment="Right" Foreground="#28a745"/>
                        </StackPanel>

                        <Button Grid.Column="3" Content="Show Details" 
                                Command="{Binding DataContext.OpenCurrencyDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource ButtonStyle}"
                                Height="35" Margin="10,0,0,0" FontSize="11"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>