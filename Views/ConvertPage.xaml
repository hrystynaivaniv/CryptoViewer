<UserControl x:Class="CryptoViewer.Views.ConverterPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="400">
    <Grid Margin="20">
        <StackPanel>
            <Button Content="← Back" Command="{Binding BackCommand}" HorizontalAlignment="Left"/>

            <TextBlock Text="Currency Converter" FontSize="24" FontWeight="Bold" Margin="0,10"/>

            <TextBlock Text="From:"/>
            <ComboBox ItemsSource="{Binding Currencies}" SelectedItem="{Binding FromCurrency}" DisplayMemberPath="Name"/>

            <TextBlock Text="Amount:"/>
            <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Height="30" VerticalContentAlignment="Center"/>

            <TextBlock Text="To:"/>
            <ComboBox ItemsSource="{Binding Currencies}" SelectedItem="{Binding ToCurrency}" DisplayMemberPath="Name"/>

            <Border Background="#F0F0F0" Padding="15" CornerRadius="5" Margin="0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Result, StringFormat={}{0:N4}}" FontSize="26" FontWeight="Bold" Foreground="#2196F3"/>
                    <TextBlock Text="{Binding ToCurrency.Symbol}" FontSize="20" Margin="10,0,0,0" VerticalAlignment="Bottom" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>