<Window x:Class="CryptoViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CryptoViewer.ViewModels"
        xmlns:v="clr-namespace:CryptoViewer.Views"
        Title="CryptoViewer" Height="600" Width="900">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:MainViewModel}">
            <v:MainCurrencyPage />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CurrencyDetailViewModel}">
            <v:CurrencyDetailPage />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>